{
  "name": "likesplan-media-grabber-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && pip install yt-dlp && apt-get update && apt-get install -y ffmpeg"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
